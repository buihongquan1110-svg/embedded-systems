; Minimal placeholder startup file for STM32F10x (for reference only)
; Replace with official vendor startup when building in Keil.
AREA |.text|,CODE,READONLY
EXPORT Reset_Handler
Reset_Handler
    B .
END
